# Core dependencies for TTS model comparison
torch>=2.1.0
torchaudio>=2.1.0
numpy
scipy
soundfile

# DirectML for AMD GPU support (Windows ThinkPad - Radeon 790M)
torch-directml

# For Seamless M4T v2
transformers>=4.40.0
sentencepiece

# For Chatterbox
chatterbox-tts

# For MeloTTS - Install from source
# WINDOWS USERS: See WINDOWS_INSTALL.md for build dependency solutions
#
# Pre-install build dependencies (avoids compilation issues):
#   pip install --upgrade pip setuptools wheel
#   pip install tokenizers fugashi[unidic-lite]
#
# Then install MeloTTS:
#   git clone https://github.com/myshell-ai/MeloTTS.git
#   cd MeloTTS
#   pip install -e .
#   python -m unidic download

# For VibeVoice Realtime 0.5B - Install from source
# git clone https://github.com/vibevoice-community/VibeVoice.git
# cd VibeVoice
# pip install -e .
# huggingface-cli download microsoft/VibeVoice-Realtime-0.5B --local-dir ./checkpoints/0.5B

# Additional dependencies that may be needed
huggingface-hub
accelerate
safetensors
